name: example
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.14.0 <3.0.0'
  flutter: '>=2.5.0'

dependencies:
  flutter:
    sdk: flutter

# Optional (default path: lib/gen/)
version_gen: 
  path: lib/gen/ 

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2
  version_gen:
    path: ../

  flutter_lints: '>=1.0.4 <2.0.0'

flutter:
  uses-material-design: true
